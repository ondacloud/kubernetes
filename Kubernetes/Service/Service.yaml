apiVersion: v1
kind: Service
metadata:
  name: <Service name>
	namespace: <namespace>
  labels:
    <key>: <value>
spec:
  selector:
    <key>: <value>
  ports:
    - protocol: <protocol>
      port: <Port>
      targetPort: <Port>